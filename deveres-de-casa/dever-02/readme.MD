# Dever 02

## Algoritmo
O algoritmo calcula o fatorial de um número utilizando recursão.

## Complexidade

A complexidade assintótica do algoritmo é:

O(n)

Isso ocorre porque a função recursiva é chamada n vezes até chegar ao caso base.

Portanto, o tempo de execução cresce linearmente conforme o tamanho da entrada.